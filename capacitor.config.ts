import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-standalone-icon-error',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
